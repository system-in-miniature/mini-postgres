failure_files = ["tests/unit/wal/test_manager.py", "tests/unit/wal/test_records.py"]

[[blocks]]
id = "mechanism"
title_en = "Checksummed WAL records mechanism"
title_zh = "带校验和的 WAL Record机制"
summary_en = "LSNs are monotonic and a record is visible only after its complete frame is flushed."
summary_zh = "LSN 单调递增，Record 只有在完整 Frame Flush 后才可见。"
files = ["src/minipostgres/wal/manager.py", "src/minipostgres/wal/records.py"]

[[blocks]]
id = "supporting"
title_en = "Package, fixture, and project support"
title_zh = "包、Fixture 与工程支撑"
summary_en = "Keep exports, test corpora, dependencies, and the runtime environment reproducible."
summary_zh = "保持包导出、测试语料、依赖与运行环境可复现。"
files = ["src/minipostgres/wal/__init__.py"]
supporting = true
